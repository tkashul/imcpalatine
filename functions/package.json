{
  "name": "imc-volunteer-planner",
  "version": "1.0.0",
  "description": "Backend for IMC Palatine Volunteer Planner",
  "main": "handler.js",
  "scripts": {
    "deploy": "zip -r function.zip handler.js shared/ api/ node_modules/ package.json && aws lambda update-function-code --function-name imc-volunteer-planner --zip-file fileb://function.zip",
    "setup-db": "node scripts/setup-db.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@aws-sdk/client-ses": "^3.600.0",
    "uuid": "^9.0.0"
  }
}
